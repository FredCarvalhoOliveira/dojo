[project]
name = "dojo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.9.0",
    "datasets>=4.0.0",
    "peft>=0.17.0",
    "transformers>=4.54.1",
]
